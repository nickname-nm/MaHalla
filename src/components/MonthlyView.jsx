// MonthlyView.jsx — member monthly overview
// Shows logs grouped by project for the selected month.
// Displays total hours per project and a grand total for the month.
// Includes month navigation (prev/next).

import React from 'react'

export default function MonthlyView({ user }) {
  return (
    <div className="p-4">
      <h2 className="text-xl font-bold mb-4">Monthly Overview</h2>
      <p className="text-gray-400">Monthly view — coming soon</p>
    </div>
  )
}
